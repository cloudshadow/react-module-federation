import * as React from 'react';

import GraphqlComponent from '@/components/GraphqlCompontent/index';

// type RootActions = ActionType<typeof rootAction>;
const GraphqlPage: React.FunctionComponent = () => {
  return (
    <div>
      <GraphqlComponent />
    </div>
  );
};
export default GraphqlPage;
